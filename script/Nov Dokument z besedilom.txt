let drops = document.getElementsByClassName("drop");
let downs = document.getElementsByClassName("down");

function createLiList(parent) {
  let dropdowns;
}

let znamkaDropowns = document.getElementsByClassName("znamka-dropdown");

let liNodeList = znamkaDropowns[0].querySelectorAll("li");
let liNodeList2 = znamkaDropowns[1].querySelectorAll("li");

const array1 = Array.from(liNodeList);
const array2 = Array.from(liNodeList2);

// Combine the arrays
const combinedArray = array1.concat(array2);

for (let i = 0; i < combinedArray.length; i++) {
  console.log(combinedArray[i].innerHTML);
}

console.log("LI length: " + liNodeList.length);
console.log(liNodeList);

///////////////////////////////////////////

console.log("LENGTH: " + drops.length);

for (let i = 0; i < drops.length; i++) {
  drops[i].addEventListener("click", function () {
    let firstChild = drops[i].children[0];
    firstChild.style.display = "block";
  });
}

document.addEventListener("click", function (event) {
  console.log(drops.length);
  for (let i = 0; i < drops.length; i++) {
    console.log(i);
    if (!drops[i].contains(event.target)) {
      let firstChild = drops[i].children[0];

      firstChild.style.display = "none";
      //  drops[0].firstChild.style.display = "none";
    }
  }
});

/*
for (let i = 0; i < lis.length; i++) {
  console.log("LIS LENGTH: " + lis.length);
  lis[i].addEventListener("click", function (event) {
    alert("WORKING  " + current.children[0].innerHTML);
  });
}*/
